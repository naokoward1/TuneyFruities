Download all included folders: assets, character_anim, main, and Sound. 
Include minim library by opening up main.pde then opening the sketch tab ->
  import library -> add library.. minim.
Press play on main.pde. 

During free play and game mode, the following keys act as controls:
	d triggers the strawberry (c_key)
	f triggers the blueberry (d_key)
	j triggers the orange (e_key)
	g triggers the melon (g_key)

The mouse allows for screen shifts by clicking the buttons.
"q" returns to the start screen.
Space bar pauses and resumes play.
